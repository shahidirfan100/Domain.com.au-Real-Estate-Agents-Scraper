{
  "title": "Domain.com.au Real Estate Agents Scraper",
  "description": "Extract Australian real estate agent profiles from Domain.com.au using fast internal JSON endpoints with API-first extraction.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Search URL",
      "type": "string",
      "description": "Domain real-estate agents search URL (location slug with postcode recommended).",
      "editor": "textfield",
      "example": "https://www.domain.com.au/real-estate-agents/sydney-nsw-2000/",
      "default": "https://www.domain.com.au/real-estate-agents/sydney-nsw-2000/",
      "prefill": "https://www.domain.com.au/real-estate-agents/sydney-nsw-2000/"
    },
    "state": {
      "title": "State Capital",
      "type": "string",
      "description": "Australian state - will default to state capital with postcode for search.",
      "editor": "select",
      "enum": [
        "nsw",
        "vic",
        "qld",
        "wa",
        "sa",
        "tas",
        "act",
        "nt"
      ],
      "enumTitles": [
        "New South Wales (Sydney)",
        "Victoria (Melbourne)",
        "Queensland (Brisbane)",
        "Western Australia (Perth)",
        "South Australia (Adelaide)",
        "Tasmania (Hobart)",
        "Australian Capital Territory (Canberra)",
        "Northern Territory (Darwin)"
      ]
    },
    "specialization": {
      "title": "Specialization",
      "type": "string",
      "description": "Filter by agent specialization or property type focus.",
      "editor": "select",
      "enum": [
        "residential",
        "commercial",
        "rural",
        "prestige",
        "rental"
      ],
      "enumTitles": [
        "Residential",
        "Commercial",
        "Rural",
        "Prestige/Luxury",
        "Rental/Property Management"
      ]
    },
    "maxResults": {
      "title": "Maximum Agents",
      "type": "integer",
      "description": "Maximum number of agent profiles to collect.",
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "maxPages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process (each page typically contains 20-30 agents).",
      "editor": "number",
      "default": 3,
      "prefill": 3
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for requests. Residential proxies strongly recommended to avoid blocking.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": []
}
